# プロジェクト概要

文書を作成するWebアプリです。

ブラウザ上でリッチテキスト形式で文書を作成します。
作成した文書はデータベースではなくマークダウン形式のファイルとして保存します。

文書を表示する際にマークダウンからHTMLに変換してレスポンスします。

アプリはDockerイメージとしてリリースされ、ユーザーはコンテナをローカルで起動して利用します。

# 技術選定

Next.jsのポートフォリオにするため、React/Next.jsを利用します。

リッチテキストを実装するライブラリはMilkdownを利用します。

# 機能詳細

## ファイルとURLの対応

環境変数 `ORENO_ROOT` で文書ファイルの **ルートディレクトリ** を指定します。

URLのホスト名からの相対パスと、ファイルパスのルートディレクトリからの相対パスが相互に対応します。

例えば、

* Orenoをポート番号 `3000` で稼動させている。
* ルートディレクトリは `/oreno` である。

という場合、

`http://localhost:3000/docs/hello`

にアクセスすると

`/oreno/docs/hello.md`

を読み取って文書を表示します。

ただし、このルールだと

`http://localhost:3000/`

にアクセスした場合のファイルパスを決められないので、このURLへのリクエストは404エラーを返します。

URLに `*` などのファイルパスの指定で特別な意味のある記号が含まれている場合も404エラーになります。

## 表示モードと編集モード

リクエストされた文書は読み取り専用で表示します。

画面にある「編集」ボタンを押すと編集可能になり、同時に表示される「保存」ボタンを押すと編集内容をファイルに保存します。

編集モードでは「削除」ボタンも表示され、これを押すとファイルを削除します。

## ページの新規作成

存在しないファイルを参照するURLにアクセスすると、編集モードで空の文書を開きます。

文書を入力して保存ボタンを押すとURLに対応するマークダウンファイルを作成します。

## 認証

ローカルでサーバーを起動するためログインは不要です。

## バージョン管理

文書はGitでバージョン管理します。

# 制約

文書はマークダウンとして保存するので、マークダウンで表現できない要素は文書に追加できません。

# 予定している機能

基本的な機能を実装してから予定している機能です。
現時点では実装しません。

## HTMLのキャッシュ

マークダウンから変換したHTMLをキャッシュすることでレスポンスをより高速にする。

## 全文検索

入力したキーワードが含まれる、または関連する文書を検索する。

## リモートアクセス

Webに公開してリモートからのアクセスを受け付ける機能。

認証機能が必要になる。

## Gitクライアント

アプリにGitクライアント機能を備える。

画面上でコミットや差分確認を行えるようにする。
